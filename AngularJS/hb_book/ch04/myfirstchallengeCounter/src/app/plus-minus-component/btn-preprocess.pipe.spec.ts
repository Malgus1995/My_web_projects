import { BtnPreprocessPipe } from './btn-preprocess.pipe';

describe('BtnPreprocessPipe', () => {
  it('create an instance', () => {
    const pipe = new BtnPreprocessPipe();
    expect(pipe).toBeTruthy();
  });
});
